import React from 'react'

export default function AndraBank(props) {
    return (
        <div>
            <h1>the main branch of AndraBank is located at {props.location}</h1>
        </div>
    );
    
}
